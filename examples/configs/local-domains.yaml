# Private Tailscale URLs plus HTTPS .local names for phones and laptops on the same Wi-Fi.
version: 1
name: greppa

services:
  web:
    target: localhost:3000
    path: /
    domain: greppa.local

  api:
    target: localhost:4000
    path: /api
    domain: api.greppa.local
